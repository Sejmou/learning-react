// the filename is always used as route pathname for all components in pages
// index.js is the only exception! just like index.html, it's kinda the default page
// it is rendered when a request directly to the domain the server runs on is made (not a subpage)

const HomePage = () => {
  return <div>HomePage</div>;
};
export default HomePage;
